**Run Backend by below command (make sure install all apckages)**
```bash
      python -m uvicorn apicall:app --reload

```
**For Frontend Run the html file. (simply double click on that html file)**
